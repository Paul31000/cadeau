<form method="post" action="{{ path('app_cadeau_delete', {'id': cadeau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cadeau.id) }}">
    <button class="btn">Supprimer</button>
</form>

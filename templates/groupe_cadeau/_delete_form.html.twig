<form method="post" action="{{ path('app_groupe_cadeau_delete', {'id': groupe_cadeau.id}) }}" onsubmit="return confirm('Etes vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe_cadeau.id) }}">
    <button class="btn">Supprimer</button>
</form>

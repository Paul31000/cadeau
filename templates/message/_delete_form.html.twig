<form method="post" action="{{ path('app_message_delete', {'id': message.id}) }}" onsubmit="return confirm('Etes-vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
    <button class="btn">Supprimer</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}UtilisateurOffre{% endblock %}

{% block body %}
    <h1>UtilisateurOffre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur_offre.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ utilisateur_offre.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateur_offre_index') }}">back to list</a>

    <a href="{{ path('app_utilisateur_offre_edit', {'id': utilisateur_offre.id}) }}">edit</a>

    {{ include('utilisateur_offre/_delete_form.html.twig') }}
{% endblock %}
